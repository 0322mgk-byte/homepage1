rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 환경: 모든 접근 허용 (프로덕션에서는 변경 필요)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
